package application;

public class Design {
	
	public static void main(String[] args) {
		
		/*
		 * Categorias de classes
		 * - em um sistema orientado a objetos, de modo geral "tudo" é objeto.
		 * - por questões de design tais como organização, flexibilidade, reuso, delegação, etc., há várias categorias de classes:
		 * Views (telas do sistema)
		 * Controllers (meio campo entre a tela e o sistema)
		 * Entities (produtos, clientes, pedidos, etc)
		 * Services 
		 * Repositories (responsaveis por acessar dados de um banco de dados)
		 */
		
	}

}
