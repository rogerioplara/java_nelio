
public class Garbage_collector {

	public static void main(String[] args) {

		/*
		 * Desalocação de memória - garbage collector e escopo local;
		 * 
		 * Garbage collector: - processo que automatiza o gerenciamento de memória de um
		 * programa de execução -> automatiza o heap; - o garbage collector monitora os
		 * objetos alocados dinamicamente pelo programa (no heap), desalocando aqueles
		 * que não estão mais sendo utilizados;
		 * 
		 * Desalocação por escopo - a memória é desalocada assim que o programa sai do
		 * escopo;
		 * 
		 * Objetos alocados dinamicamente, quando não possuem mais referência para eles,
		 * serão desalocados pelo garbage collector; Variáveis locais são desalocadas
		 * imediatamente assim que seu escopo local sai de execução;
		 */

	}

}
